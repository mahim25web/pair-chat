# Pair Chat

A simple GitHub-ready project with an `index.html` file.

## Getting started

1. Add a GitHub remote:
   ```bash
git remote add origin https://github.com/<username>/<repo>.git
```
2. Push to GitHub:
   ```bash
git push -u origin main
```
